import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView className="bg-white flex-1 h-full w-full">
      <Text>Home</Text>
    </SafeAreaView>
  );
};

export default Home;
